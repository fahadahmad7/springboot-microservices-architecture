server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/fahadahmad7/springboot-microservices-architecture.git
          search-paths: config-repo
          default-label: main
